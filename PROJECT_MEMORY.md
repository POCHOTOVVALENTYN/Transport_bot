# Проєкт: Transport_bot — Пам'ять Проєкту та Архітектура

У цьому файлі зафіксовано архітектурні рішення, конфігурацію сервера та зміни у логіці бота для забезпечення наступності розробки.

---

## 1. Архітектурні рішення та Міграція

### 1.1 Перехід на SQLite та bare-metal
* **Проблема**: Oracle Cloud Free Tier VM має обмежений об'єм оперативної пам'яті (1 ГБ). Спроби запускати базу даних PostgreSQL та бота у Docker-контейнерах призводили до OOM (Out Of Memory) крашів системи.
* **Рішення**:
  * Проєкт мігрував на прямий запуск (bare-metal) через системну службу `systemd`.
  * Замість PostgreSQL підключено базу даних **SQLite**, що значно економить ресурси та забезпечує миттєвий відгук.
  * База даних на продакшні зберігається безпосередньо на диску в робочій директорії (файл `transport_bot.db`).
* **Конфігурація `DATABASE_URL` у `.env`**:
  ```env
  DATABASE_URL=sqlite+aiosqlite:///transport_bot.db
  ```

### 1.2 Налаштування systemd та SELinux
* **Директорія розгортання**: `/opt/transport_bot` (замість `/home/opc` через обмеження доступу).
* **Конфігурація служби systemd** (`/etc/systemd/system/transport-bot.service`):
  ```ini
  [Unit]
  Description=Transport Bot Service
  After=network.target

  [Service]
  Type=simple
  User=opc
  WorkingDirectory=/opt/transport_bot
  ExecStart=/opt/transport_bot/venv/bin/python main.py
  Restart=always
  RestartSec=5
  EnvironmentFile=/opt/transport_bot/.env

  [Install]
  WantedBy=multi-user.target
  ```
* **SELinux**:
  * Для уникнення помилки запуску `203/EXEC` та блокування доступу до файлів конфігурації, системні дописи було адаптовано, а також тимчасово вимкнено або пом'якшено примусове блокування SELinux (`setenforce 0` / permissive mode), якщо це викликало проблеми з доступом служби.

---

## 2. Логіка реєстрації у Музеї та синхронізація

### 2.1 Локальне сховище замість прямого запису в Google Sheets
* **Проблема**: Пряма робота з API Google Sheets під час взаємодії користувача з ботом створювала мережеві затримки та тайм-аути, через що користувачі не могли завершити бронювання, а бот підвисав.
* **Рішення**:
  * Дані про нове бронювання миттєво зберігаються у локальну таблицю SQLite `museum_bookings`.
  * Бот негайно підтверджує успішну реєстрацію користувачу.
  * Відразу після цього у фоні запускається асинхронна задача `_sync_to_sheets_task(booking_id)`, яка додає запис у Google Sheets (таблиця `MuseumBookings`).
  * Якщо фонова синхронізація з якихось причин не вдалася, статус запису в БД залишається `"new"`, і його можна синхронізувати пізніше за допомогою методу `sync_unsynced_bookings()`.

### 2.2 Перегляд списку зареєстрованих для адміністратора
* **Проблема**: Кнопка «Перелік зареєстрованих» в адмін-панелі раніше зчитувала всі рядки безпосередньо з Google Sheets, що працювало повільно.
* **Рішення**:
  * У `services/museum_service.py` метод `get_last_bookings()` переписано на зчитування останніх 50 записів із локальної SQLite бази.
  * Це забезпечує миттєве відкриття списку для адміністраторів музею без затримок.

### 2.3 Форматування часу: Київська часова зона
* **Проблема**: База даних автоматично проставляє час створення запису через `func.now()`, що використовує системний час (UTC). При завантаженні у Google Sheets або виводі в адмінку час відображався у форматі UTC.
* **Рішення**:
  * У `services/museum_service.py` реалізовано конвертацію в часовий пояс Києва:
    ```python
    def _to_kyiv_time(self, dt_value):
        if not dt_value: return None
        from datetime import timezone
        from zoneinfo import ZoneInfo
        if dt_value.tzinfo is None:
            dt_value = dt_value.replace(tzinfo=timezone.utc)
        return dt_value.astimezone(ZoneInfo("Europe/Kyiv"))
    ```
  * Перед відправкою у Google Sheets та перед показом адміністратору час конвертується через `_to_kyiv_time` та форматується як `ДД.ММ.РРРР ГГ:ХХ`.

---

## 3. Концепція середовища розробки та тестування (Dev/Test Environment)

Для безпечного додавання нових функцій та їх тестування окремо від активних користувачів пропонується наступна схема ізоляції на тому ж сервері (1 GB Oracle VM):

### 3.1 Розділення Telegram-ботів
* **Production Bot**: Працює на основному токені (напр., `@OdesaTransportBot`).
* **Dev/Test Bot**: Створюється окремий бот через `@BotFather` (напр., `@OdesaTransportDevBot`) з унікальним токеном `TELEGRAM_BOT_TOKEN`. Всі нові фічі тестуються виключно через нього.

### 3.2 Гілки у Git (Git Flow)
* `main` — стабільна продакшн-версія. Будь-які зміни сюди потрапляють тільки після успішного тестування.
* `dev` — гілка розробки. Сюди зливаються нові функції для тестування на сервері.

### 3.3 Ізоляція файлової системи та конфігурації
На сервері створюється паралельна директорія для dev-версії:
* Продакшн: `/opt/transport_bot`
* Dev: `/opt/transport_bot_dev`

Кожна директорія має свій незалежний файл `.env`:
* **У продакшн `.env`**:
  * Робочий токен бота.
  * Основна БД: `DATABASE_URL=sqlite+aiosqlite:///transport_bot.db`.
  * Основний Google Sheets ID.
  * `DEBUG=False`.
* **У dev `.env`**:
  * Тестовий токен бота.
  * Тестова БД: `DATABASE_URL=sqlite+aiosqlite:///transport_bot_dev.db`.
  * Тестова Google Sheets таблиця (копія основної) — для перевірки синхронізації без забруднення реальних звітів.
  * `DEBUG=True` (для детального виведення помилок в консоль).

### 3.4 Окрема служба systemd
Для Dev-бота створюється окремий системний сервіс `/etc/systemd/system/transport-bot-dev.service`:
```ini
[Unit]
Description=Transport Bot - Development Service
After=network.target

[Service]
Type=simple
User=opc
WorkingDirectory=/opt/transport_bot_dev
ExecStart=/opt/transport_bot_dev/venv/bin/python main.py
Restart=always
RestartSec=5
EnvironmentFile=/opt/transport_bot_dev/.env

[Install]
WantedBy=multi-user.target
```

### 3.5 Скрипт автоматичного деплою на Dev
Для швидкого оновлення dev-версії створюється скрипт `/opt/transport_bot_dev/deploy_dev.sh`:
```bash
#!/bin/bash
cd /opt/transport_bot_dev
git checkout dev
git pull origin dev
./venv/bin/pip install -r requirements.txt
sudo systemctl restart transport-bot-dev
echo "🚀 Dev-версія оновлена та перезапущена!"
```


---

## 4. Оновлення від 2026-09-14: 4 невеликі фічі

Цей розділ фіксує реалізацію 4 точкових оновлень, узгоджених з користувачем (аналіз + уточнюючі питання + план виконано перед стартом). Усі зміни зроблено безпосередньо на робочому дереві (`/opt/transport_bot` на сервері / локальна копія `Transport_bot`), без запуску реального venv у сесії розробки (див. примітку про тестування в кінці розділу).

### 4.1 Пільговий проїзд — нова категорія
* **Що зроблено**: у `config/messages.py`, ключ `tickets_benefits`, у блок `"👨‍👩‍👧‍👦 Діти та Сім'ї"` додано новий рядок `"- Учні 1-11 класів Одеських шкіл"` (одразу після `"- Діти до 7 років"`).
* **Файли**: `config/messages.py`.
* **Ризик**: мінімальний, суто текстова зміна.

### 4.2 Вакансії — перехід на адмін-керовану БД
* **Проблема раніше**: список вакансій (2 категорії — "з досвідом" / "без досвіду") був захардкоджений у словниках `EXPERIENCED_VACANCIES`/`TRAINEE_VACANCIES` прямо в `handlers/company_handlers.py`. Дві вакансії з публічних закупівель ("провідний фахівець публічних закупівель", "фахівець публічних закупівель") видалено окремим комітом того ж дня ще до старту цієї роботи.
* **Рішення**: додано нову таблицю `vacancies` (модель `Vacancy` у `database/db.py`) з полями `category` ("experienced"/"trainee"), `title`, `contact_type` ("url"/"phone"), `contact_value`, `sort_order`, `is_active` (soft-delete), `admin_id`/`admin_name` (хто додав).
  * При першому старті `init_db()` виконує одноразовий seed: усі 10 попередніх захардкоджених вакансій переносяться в таблицю зі збереженням порядку (`sort_order`).
  * Новий сервіс `services/vacancy_service.py` (`VacancyService`): `create_vacancy`, `get_active_vacancies(category)` (клієнтський показ), `get_admin_vacancies(...)` (адмін-список з пагінацією), `get_vacancy_by_id`, `deactivate_vacancy`, `reactivate_vacancy`.
  * `handlers/company_handlers.py` → `show_vacancy_list` тепер читає вакансії з БД через `vacancy_service.get_active_vacancies`, а не зі словника.
  * **Знайдений і виправлений попутний баг**: контакт вакансії "Спеціаліст нарядник поїзних бригад" зберігався як голий номер телефону `+380751705557` і використовувався напряму в `InlineKeyboardButton(url=...)` — Telegram вимагає валідну схему URL, тому такий номер, ймовірно, не спрацьовував би. Виправлено: для `contact_type="phone"` кнопка формується як `tel:+380751705557`.
  * **Адмін-панель**: новий пункт меню "👔 Вакансії" в загальному адмін-меню (`show_general_admin_menu`). Функціонал: перегляд активних вакансій за категоріями з пагінацією, картка вакансії, деактивація (soft-delete, без фізичного видалення — за вибором користувача), додавання нової вакансії через `ConversationHandler` (категорія → назва → тип контакту → значення контакту з валідацією URL/телефону).
  * Нові стани: `ADMIN_VACANCY_CATEGORY`, `ADMIN_VACANCY_TITLE`, `ADMIN_VACANCY_CONTACT_TYPE`, `ADMIN_VACANCY_CONTACT_VALUE` (`bot/states.py`).
  * Доступ — ті самі `GENERAL_ADMIN_IDS`, що й для інших адмін-функцій (загублені речі, новини тощо).
* **Файли**: `database/db.py`, `services/vacancy_service.py` (новий), `handlers/company_handlers.py`, `handlers/admin_handlers.py`, `bot/bot.py`, `bot/states.py`.

### 4.3 Загублені речі — редагування вже створеного запису
* **Контекст**: адмін-CRUD для загублених речей уже існував повністю (додавання, перегляд, позначення поверненою, видалення, архів). Бракувало лише можливості виправити помилку в назві/деталях без видалення й повторного створення запису.
* **Рішення**: додано метод `update_lost_item(item_id, title=None, details=None)` у `services/lost_items_service.py`. У картці знахідки (`admin_lost_view`) додано кнопку "✏️ Редагувати". Новий `ConversationHandler` (`admin_lost_edit_conv`) з вибором поля (назва/деталі) → введенням нового значення → оновленою карткою з кнопкою "✏️ Редагувати ще раз".
  * Нові стани: `ADMIN_LOST_EDIT_CHOICE`, `ADMIN_LOST_EDIT_TITLE`, `ADMIN_LOST_EDIT_DETAILS` (`bot/states.py`).
* **Файли**: `services/lost_items_service.py`, `handlers/admin_handlers.py`, `bot/bot.py`, `bot/states.py`.

### 4.4 Музей КП ОМЕТ — ліміти для звичайної екскурсії ("Запис на екскурсію")
* **Було**: вкладка "Запис на святкову екскурсію" вже мала ліміти (40 людей на дату / 2 на один запис) та збір ПІБ+вік кожного учасника. Вкладка "Запис на екскурсію" (звичайна) таких лімітів не мала — приймала довільну кількість людей і лише одне ім'я контактної особи.
* **Рішення** (за узгодженим з користувачем рішенням): для звичайної екскурсії застосовано ту саму логіку, що й для святкової, але з іншими лімітами і **без збору віку**:
  * Нові константи `MuseumLimits` (`config/constants.py`): `HOLIDAY_MAX_TOTAL=40`, `HOLIDAY_MAX_PER_BOOKING=2`, `REGULAR_MAX_TOTAL=60`, `REGULAR_MAX_PER_BOOKING=5`.
  * Нова колонка `participants_details` (TEXT, JSON-масив) у таблиці `museum_bookings` (міграція `ALTER TABLE` в `init_db()`), уже використовувана святковою екскурсією — тепер заповнюється і для звичайної.
  * `handlers/museum_handlers.py`: гілки `excursion_type == 'holiday'` дублюються паралельними гілками `excursion_type == 'regular'` у ключових функціях (`museum_edit_field`, `museum_get_date` — перевірка ліміту 60/5, `museum_get_people_count`, `museum_get_participant_name` — для 'regular' зберігається лише `name`, `age` завжди `None`, без питання про вік, `museum_get_phone_input` — перевірка дубля телефону на дату, `museum_confirm_save` — повторна перевірка ліміту перед збереженням + збереження `participants_details`).
  * `services/museum_service.py`: `create_booking` приймає новий опційний параметр `participants_details`; `get_last_bookings` форматує ПІБ учасників без віку для звичайної екскурсії; додано `get_bookings_count(date)` та `has_existing_booking(date, phone)`.
  * **Знайдений і виправлений попутний баг**: у сповіщенні адміну (`museum_confirm_save`) формування рядка з учасниками робило `f"{p['name']} ({p['age']} р.)"` без перевірки — для звичайної екскурсії, де `age is None`, це давало б `"(None р.)"`. Виправлено умовним форматуванням (вік показується, лише якщо він заданий).
  * **Відома дрібна прогалина (не критична, поза межами цього завдання)**: адмінська утиліта масової синхронізації `sync_unsynced_bookings()` у `museum_service.py` НЕ оновлена для включення імен учасників у ручний Google Sheets sync — там і раніше йшло лише ім'я контактної особи. Якщо знадобиться — доробити окремо.
* **Файли**: `config/constants.py`, `database/db.py`, `services/museum_service.py`, `handlers/museum_handlers.py`.

### 4.5 Тестування
* Створено `tests/test_museum_regular.py` (за зразком наявного `tests/test_museum_holiday.py`) — 8 юніт-тестів на лічильник бронювань, перевірку дубля телефону, створення бронювання з 5 учасниками (ПІБ без віку), форматування рядка учасників, ліміт місткості.
* **Важливо**: середовище, у якому виконувалась ця розробка, не мало мережевого доступу й встановлених залежностей (`sqlalchemy`, `aiosqlite`, `python-telegram-bot`), тому всі зміни перевірено лише синтаксично (`python3 -m py_compile` — успішно для всіх файлів проєкту). **Перед деплоєм обов'язково запустити на сервері/локально з реальним venv**:
  ```bash
  python3 tests/test_museum_holiday.py
  python3 tests/test_museum_regular.py
  ```
  а також вручну перевірити нові адмін-сценарії (вакансії, редагування знахідок) через тестового бота.


---

## 5. Оновлення від 2026-09-24: Формування офіційного файлу відвідувачів музею (DOCX)

Реалізовано можливість для співробітників/адміністраторів музею КП "ОМЕТ" формувати та завантажувати офіційний бланк-відомість відвідувачів на кожну конкретну дату екскурсії (як для стандартної, так і для святкової) безпосередньо в Telegram-боті.

### 5.1 Вимоги до формату документу (ДСТУ 4163:2020)
* **Формат файлу**: Microsoft Word (`.docx`), що формується в оперативній пам'яті через `io.BytesIO` бібліотекою `python-docx`. Не потребує встановлених системних шрифтів на Linux-сервері.
* **Шрифт**: `Times New Roman`, розмір `14 pt`.
* **Міжрядковий інтервал**: `1.15`, відступи абзацу: `space_after = 0`, `space_before = 0`.
* **Вирівнювання**: по ширині (`WD_ALIGN_PARAGRAPH.JUSTIFY`).
* **Орієнтація**: альбомна (`Landscape`, 297 x 210 мм) для забезпечення комфортного розміщення 5 колонок без нагромадження тексту.
* **Межі таблиці**: суцільна тонка чорна рамка для всіх зовнішніх та внутрішніх комірок, захист від розриву рядків (`cantSplit`), дублювання шапки таблиці на наступних сторінках (`tblHeader`).

### 5.2 Шапка та структура таблиці
* **Шапка документа**:
  ```
  СПИСОК ЗАРЕЄСТРОВАНИХ ВІДВІДУВАЧІВ
  НА СТАНДАРТНУ ЕКСКУРСІЮ [або НА СВЯТКОВУ ЕКСКУРСІЮ]
  ДО МУЗЕЮ КП “ОМЕТ”
  (м. Одеса, пл. Олексіївська, 1А)
  ДД.ММ.РРРР
  ```
* **Колонки таблиці**:
  1. `№ п/п` (наскрізна нумерація 1, 2, 3...)
  2. `Дата реєстрації на екскурсію`
  3. `П.І.Б. зареєстрованих відвідувачів`
  4. `Контактний номер телефону`
  5. `Згода на гарантування дотримання правил з техніки безпеки та поведінки на території підвищеної небезпеки` (порожня комірка під власноручний підпис відвідувача).
* **Групові бронювання**: кожен учасник групи (до 5 на стандартну, до 2 на святкову) розгортається в **окремий рядок таблиці з власним номером п/п** під особистий підпис з ТБ на території депо.
* **Підсумковий блок**:
  * «Всього зареєстровано відвідувачів: X осіб.»
  * «Відповідальний за проведення інструктажу з ТБ: ___________________ / ___________________ /»

### 5.3 Джерело даних та відмовостійкість
* Пріоритет: читання з Google Sheets (`MuseumBookings` / `Holiday excursion list`) для врахування можливих ручних правок співробітників у веб-таблиці.
* Fallback: при мережевій помилці або відсутності зв'язку з Google API дані миттєво підтягуються з локальної SQLite БД (`museum_bookings` / `museum_holiday_bookings`).

### 5.4 Адмін-інтерфейс у Telegram
* У меню адміна музею (`get_admin_main_menu_keyboard`) додано дві кнопки:
  * `📄 Сформувати список (Звичайна)` (`admin_export_doc_select:regular`)
  * `🎉📄 Сформувати список (Святкова)` (`admin_export_doc_select:holiday`)
* Адміністратор натискає кнопку -> отримує перелік дат, на які є бронювання -> натискає потрібну дату -> бот надсилає згенерований документ `.docx` із супровідним описом.

### 5.5 Файли та залежності
* `requirements.txt`, `requirements-server.txt`: додано `python-docx>=1.1.0`.
* `services/museum_document_service.py`: новий сервіс генерації DOCX-файлів за заданими стандартами.
* `services/museum_service.py`: додано `get_visitors_for_date`, `get_distinct_booking_dates`, `_parse_participant_names`.
* `handlers/command_handlers.py`: додано кнопки експорту в клавіатуру.
* `handlers/admin_handlers.py`: додано `admin_export_doc_select` та `admin_export_doc_generate`.
* `bot/bot.py`: зареєстровано обробники `admin_export_doc_select` та `admin_export_doc_generate`.
* `tests/test_museum_document.py`: 4 юніт-тести для перевірки парсера імен, вибірки відвідувачів та генерації документа.

